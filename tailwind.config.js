const { colors } = require("tailwindcss/defaultTheme");

module.exports = {
  theme: {
    extend: {
      colors: {
        primary: colors.purple["600"],
      },
    },
  },
  variants: {},
  plugins: [],
};
